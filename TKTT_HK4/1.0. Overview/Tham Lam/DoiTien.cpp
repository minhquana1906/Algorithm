#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n, t, res = 0;
    cin >> n >> t;
    int x[n];
    for (int i = 0; i < n; i++)
    {
        cin >> x[i];
    }
    while (t--)
    {
        }
}