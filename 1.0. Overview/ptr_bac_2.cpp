#include <bits/stdc++.h>
using namespace std;

int main()
{
    // complex<double> a, b(4, 5), c = 7, k = 0;
    // cout << "\na = " << a;
    // cout << "\nb = " << b;
    // cout << "\nc = " << c;
    // cout << "\nk = " << k;
    /*
        complex<double> a, b, c, d, x1, x2, k = 0;
        cout << "\na = ";
        cin >> a;
        cout << "\nb = ";
        cin >> b;
        cout << "\nc = ";
        cin >> c;

        if (a == k)
        {
            if (b == k)
            {
                cout << (c == k ? "vo so ngiem" : "vo nghiem");
            }
            else
            {
                cout << "x= " << c / b;
            }
        }
        else
        {
            b /= 2;
            d = sqrt(b * b - a * c);
            cout << "\nx1 = " << (b - d) / a;
            cout << "\nx2 = " << (b + d) / a;
        }
        // input:
        // a = (1,0)
        // b = (2,3)
        // c = (0,1)
        */

    //    Phuong trinh trung phuong

    // ax^4 +bx^2+c=0
}
